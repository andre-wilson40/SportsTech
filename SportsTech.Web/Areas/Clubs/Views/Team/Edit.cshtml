@model SportsTech.Web.Areas.Clubs.ViewModels.Team.CreateViewModel

@{
    ViewBag.Title = "Edit team";
}

@using (Html.BeginForm("Edit", "Team", FormMethod.Post, new { id = "EditRecord" }))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true)

        @Html.Partial("_TeamForm", Model)

        <div class="form-group">
            <div class="col-xs-offset-2 col-xs-10">
                <input id="cancel-create-btn" type="submit" value="Save changes" class="btn btn-primary" />
                <input id="cancel-event-btn" type="button" value="Cancel" class="btn btn-default btn-cancel" />
            </div>
        </div>
    </div>
}

@section scripts
{
    <script>
        $(function() {
            $('#cancel-event-btn').on('click', function () {
                site.core.redirect('@Url.Action("List", "Team")');
            });
        });
    </script>
}